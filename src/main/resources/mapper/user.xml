<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.iwill.mybatis.mapper.UserMapper">
    <resultMap id="userResultMap" type="com.iwill.mybatis.bean.User">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
    </resultMap>
    <select id="findUserListByName" parameterType="com.iwill.mybatis.bean.User" resultMap="userResultMap">
        SELECT * FROM USER WHERE name =${name}
    </select>
</mapper>